<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.myhuiban.mapper.UserMapper">
    <select id="findByUsername" resultType="com.myhuiban.model.User">
        SELECT * FROM user WHERE userName = #{userName}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(uniqueId, userName, email, institution, registrationTime, password)
        VALUES (#{uniqueId}, #{userName}, #{email}, #{institution}, #{registrationTime}, #{password})
    </insert>
</mapper>
