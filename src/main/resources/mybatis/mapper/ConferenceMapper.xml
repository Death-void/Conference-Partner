<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.myhuiban.mapper.ConferenceMapper">
    <select id="findAll" resultType="com.myhuiban.model.Conference">
        SELECT * FROM conference
    </select>

    <select id="findById" resultType="com.myhuiban.model.Conference">
        SELECT * FROM conference WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO conference( name, abbreviation, CCF, CORE, QUALIS, submission_deadline, notification_date, conference_date, location, frequency)
        VALUES (#{name}, #{abbreviation}, #{CCF}, #{CORE}, #{QUALIS}, #{submissionDeadline}, #{notificationDate}, #{conferenceDate}, #{location}, #{frequency})
    </insert>

    <update id="update">
        UPDATE conference
        SET name=#{name}, abbreviation=#{abbreviation}, CCF=#{CCF}, CORE=#{CORE}, QUALIS=#{QUALIS},
            submission_deadline=#{submissionDeadline}, notification_date=#{notificationDate}, conference_date=#{conferenceDate},
            location=#{location}, frequency=#{frequency}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM conference WHERE id = #{id}
    </delete>
</mapper>
